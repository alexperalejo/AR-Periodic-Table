// Assets/Scripts/Debug/HideFilterBar.cs
//
// Previously hid the bottom FilterBar at runtime; now disabled. The category
// filter buttons render normally again.
//
// File kept (rather than deleted) so the .meta file's GUID stays stable in
// case it's referenced anywhere. Free to delete if you prefer.

using UnityEngine;

namespace PeriodicAR.Debugging
{
    public class HideFilterBar : MonoBehaviour
    {
        // Bootstrap removed — script is inert. To restore the hide behavior,
        // see git history / earlier version of this file.
    }
}
